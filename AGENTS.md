# Codex Instructions

This project is organized Claude-first.

Follow `CLAUDE.md` as the canonical project instruction file. Read the context
files it routes to before editing model code, export scripts, validation tests,
or experiments.

Codex-specific expectations:

- Prefer small, testable changes.
- State the validation target before editing model code.
- Use `rg` for repository search.
- Run relevant tests after changing source code.
